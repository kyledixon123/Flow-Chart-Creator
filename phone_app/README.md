# Phone App

This is a simple React Native phone application example. It shows a text input
for entering a phone number and a call button that logs the number to the console.

To run on a device, install React Native tooling and use `npm start`.
